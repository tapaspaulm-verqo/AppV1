bin/
obj/
node_modules/
.dart_tool/
build/
*.user
.DS_Store

# Angular CLI's local build cache — machine-specific, regenerated by every
# `ng build`/`ng serve`, and includes binary files (angular-compiler.db)
# that just produce noisy diffs if tracked.
.angular/cache/
